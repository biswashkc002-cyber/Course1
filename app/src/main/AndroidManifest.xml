<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name="com.kcbiswash.course1.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Course1">

        <activity android:name="com.kcbiswash.course1.ui.details.DetailsActivity" android:exported="false" />
        <activity android:name="com.kcbiswash.course1.ui.dashboard.DashboardActivity" android:exported="false" />
        <activity android:name="com.kcbiswash.course1.ui.login.LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
